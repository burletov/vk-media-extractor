<p align="center">
  <img src="docs/logo.png" width="180" alt="MediaTidy">
</p>

# MediaTidy

[![Release](https://img.shields.io/github/v/release/burletov/MediaTidy)](https://github.com/burletov/MediaTidy/releases/latest)
[![CI](https://github.com/burletov/MediaTidy/actions/workflows/ci.yml/badge.svg)](https://github.com/burletov/MediaTidy/actions/workflows/ci.yml)
[![License](https://img.shields.io/github/license/burletov/MediaTidy)](LICENSE)
[![Windows](https://img.shields.io/badge/Windows-10%2F11-0078D4)](https://github.com/burletov/MediaTidy/releases/latest)

**MediaTidy** — Windows-приложение для импорта, поиска дубликатов и безопасной разборки больших архивов фото и видео.

Один сценарий: импортируете медиа из VK, сканируете папки, быстро находите дубликаты и похожие изображения, сравниваете файлы в интерфейсе и переносите лишнее в карантин без безвозвратного удаления.

[English version](README.en.md)

## Главное

- **Импорт всех фото и видео из VK в несколько кликов.** Личные сообщения, беседы, отдельный чат, фото, видео и GIF. Импорт можно остановить и продолжить позже.
- **Распознавание дубликатов.** Точные копии фото и видео ищутся по SHA-256, похожие изображения группируются как кандидаты на проверку.
- **Удобная работа с файлами.** Таблица, фильтры, сортировка по столбцам, предпросмотр, сравнение рядом, карантин, восстановление и CSV-отчёт.
- **Поддержка больших архивов.** Несколько папок, объединение в общую медиатеку, быстрый выбор кандидатов и экспорт отчёта до любых файловых операций.
- **Тёмная тема.** Не главная причина ставить приложение, но приятно пользоваться вечером.

MediaTidy не удаляет файлы автоматически. Пользователь сам решает, что отправить в карантин или оставить.

## Скачать

1. Откройте [последний релиз](https://github.com/burletov/MediaTidy/releases/latest).
2. Скачайте `MediaTidy.exe` и `SHA256SUMS.txt`.
3. Запустите `MediaTidy.exe`. Установка не требуется.

Windows SmartScreen может показать предупреждение, потому что приложение пока не подписано коммерческим сертификатом. Проверяйте, что файл скачан из `burletov/MediaTidy`, и при необходимости сверяйте SHA-256:

```powershell
Get-FileHash .\MediaTidy.exe -Algorithm SHA256
```

## Быстрый старт

1. Нажмите **Выбрать папки...** и добавьте одну или несколько папок.
2. Нажмите **Сканировать**.
3. Используйте фильтры **Точные дубликаты**, **Похожие фото и видео** и категории.
4. Сравните спорные файлы через предпросмотр или режим сравнения рядом.
5. Перед перемещением экспортируйте отчёт, затем отправьте лишнее в карантин.

## Импорт из VK

1. Откройте **Файл → Импорт из VK**.
2. Укажите токен VK, папку назначения и типы файлов: фото, видео, GIF.
3. Выберите импорт всех диалогов и бесед либо одного чата.
4. Для одного чата скопируйте ID чата из адресной строки VK.
5. При необходимости включите ограничение размера файла.
6. Запустите импорт. Если процесс остановить, следующий запуск продолжит работу с сохранённой контрольной точки.

Полная выгрузка большого аккаунта может занять десятки часов. Не удаляйте служебные файлы `.mediatidy-vk-*` и незавершённые `.part`, если планируете продолжить импорт.

MediaTidy является неофициальным приложением и не связано с VK. Пользователь сам отвечает за соблюдение правил VK и прав на загружаемые материалы. Кнопка получения токена открывает сторонний сервис VKHost; проверяйте адрес сайта и разрешения токена самостоятельно.

## Возможности

- фото и видео в одной таблице;
- точные дубликаты по SHA-256;
- поиск визуально похожих файлов;
- выбор кандидатов на карантин для точных и похожих групп;
- предпросмотр файла и открытие исходной папки;
- сравнение двух изображений рядом с синхронным масштабом;
- рекомендация лучшего кадра в группе по резкости и признакам лица;
- локальные категории: фото, скриншот, мем, документ, чек, графика, размытое изображение;
- OCR, EXIF и признаки имени файла как подсказки при классификации;
- сохранённые профили сканирования;
- несколько исходных папок и общая медиатека;
- карантин с восстановлением;
- CSV-отчёт до перемещения файлов;
- светлая и тёмная темы.

Категории и оценки модели являются подсказками, а не основанием для автоматического удаления.

## Системные требования

- Windows 10 или Windows 11, x64;
- минимум 4 ГБ ОЗУ, рекомендуется 8 ГБ;
- около 400 МБ свободного места для приложения;
- интернет нужен только для импорта VK и загрузки дополнительных моделей.

Обычное сканирование, OCR и распознавание выполняются локально на компьютере.

## Конфиденциальность

- выбранные фото и видео не отправляются разработчику;
- пути, результаты анализа и токен VK не отправляются на сервер MediaTidy;
- токен VK используется только для запросов к API VK и не сохраняется приложением;
- телеметрия и рекламные SDK отсутствуют.

Подробнее: [PRIVACY.md](PRIVACY.md).

## Сборка из исходников

Потребуются Windows 10/11, [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0) и PowerShell.

```powershell
git clone https://github.com/burletov/MediaTidy.git
cd MediaTidy
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\download-model.ps1
dotnet restore .\MediaTidy.sln
dotnet run --project .\tests\MediaTidy.SmokeTests\MediaTidy.SmokeTests.csproj -c Release
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-release.ps1
```

Готовый автономный EXE и контрольная сумма появятся в `artifacts`.

## Структура проекта

- `src/MediaTidy` — WinForms-приложение;
- `tests/MediaTidy.SmokeTests` — функциональная smoke-проверка;
- `scripts` — загрузка модели и воспроизводимая сборка;
- `docs` — логотип и продуктовые материалы;
- `.github/workflows` — CI и публикация релизов по тегам `v*`.

## Релизы

Готовые пользовательские сборки публикуются в [GitHub Releases](https://github.com/burletov/MediaTidy/releases). Для каждого EXE прикладывается `SHA256SUMS.txt`. История изменений: [CHANGELOG.md](CHANGELOG.md).

## Благодарность автору

Если MediaTidy сэкономил вам время и вы хотите поблагодарить автора за работу, проект можно поддержать:

| Валюта и сеть | Адрес |
| --- | --- |
| BTC | `115M2wYM1UJ5RDQi2BBsc41R3XaTNxyLjp` |
| ETH, ERC-20 | `0xc3fc88bb6b415a1822b7989df2865795f5351c97` |
| USDT, Tron TRC-20 | `TH2UKCXXfdPYxsgxcp1BtPijkEDZWAo3Km` |
| USDT, BNB Smart Chain BEP-20 | `0xc3fc88bb6b415a1822b7989df2865795f5351c97` |

Переводы необратимы. Перед отправкой проверьте валюту, сеть и адрес.

## Лицензия

Исходный код распространяется по лицензии [MIT](LICENSE). Сторонние компоненты и модели имеют собственные условия: [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).
